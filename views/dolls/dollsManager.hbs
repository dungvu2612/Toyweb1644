<!DOCTYPE html>
<html>
<head>
  <title>{{title}}</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  <style>
    .doll-manager {
      margin-top: 20px;
      margin-bottom: 20px;
    }

    .doll-manager th {
      font-weight: bold;
    }

    .doll-manager .edit-button {
      color: white;
      background-color: green;
      border: none;
    }

    .doll-manager .delete-button {
      color: white;
      background-color: red;
      border: none;
    }
    .doll-manager .add-button {
      color: white;
      background-color: rgba(263,63,63);
      border: none;
    }
    .custom-header {
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
 <br>
  <br>
  <br>
  <div class="container doll-manager">
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th colspan="8" class="text-center">
            <h1 class="text-primary">DOLL MANAGER</h1>
          </th>
        </tr>
        <tr>
          <th colspan="2">
            <form action="/doll/manager/search" method="post">
              <input class="form-control" type="search" name="keyword" placeholder="Search by name" required>
            </form>
          </th>
          <th>
            <a class="btn add-button" href="/doll/add">✚</a>
          </th>
        </tr>
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Price</th>
          <th>Image</th>
          <th>Quantity</th>
          <th>Best Seller</th>
          <th>Category</th>
          <th>Menu</th>
        </tr>
      </thead>
      <tbody>
        {{#each dolls }}
        <tr>
          <td>{{ name }}</td>
          <td>{{ description}}</td>
          <td>{{ price }}</td>
          <td>
            <img src="{{ imageUrl }}" width="100" height="100" alt="{{ name }}">
          </td>
          <td>{{ quantity }}</td>
          <td>{{ bestSeller }}</td>
          <td>{{ category }}</td>
          <td>
            <a class="btn edit-button" href="/doll/edit/{{ _id }}">✎</a>
            <a class="btn delete-button" href="/doll/delete/{{ _id }}" onclick="return confirm('Are you sure to delete this doll?');">🗑️</a>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</body>
</html>
