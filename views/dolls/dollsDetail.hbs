<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Doll Detail</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/css/bootstrap.min.css">
  <style>
    .doll-detail {
      border: 1px solid #ccc;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .doll-image {
      max-width: 100%;
      height: auto;
    }

    .back-button {
      margin-top: 20px;
    }
  </style>
</head>

<body>
    <br>
    <br>
    
  <div class="container mt-5">
    <h1 class="text-center">Doll Detail</h1>
    <div class="row">
      <div class="col-md-6">
        <div class="doll-detail">
          <img src="{{doll.imageUrl}}" class="doll-image" alt="{{doll.name}}">
        </div>
      </div>
      <div class="col-md-6">
        <div class="doll-detail">
          <h2 class="mb-3">{{doll.name}}</h2>
          <p><strong>Description: </strong>{{doll.description}}</p>
          <div class="row">
            <div class="col-md-6">
              <p><strong>Price: {{doll.price}} USD</strong></p>
              <p><strong>Quantity:{{doll.quantity}}</strong> </p>
            </div>
            <div class="col-md-6">
              <p><strong>Best Seller: {{#if doll.bestSeller}}Yes{{else}}No{{/if}}</strong></p>
              <!-- Add more details here if needed -->
            </div>
          </div>
          <div class="back-button">
            <a href="#" class="btn btn-primary">Add to Cart</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

</html>
